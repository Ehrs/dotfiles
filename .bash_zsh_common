export EDITOR=vim
export VISUAL="$EDITOR"
export PAGER="less --quit-if-one-screen --no-init"
export LANG=en_US.UTF-8
export PATH=~/bin:${PATH}
export PATH=$PATH:/usr/local/texlive/2016/bin/x86_64-linux
export HOMEBREW_GITHUB_API_TOKEN=eed29bfb7b1c848ac3a7c620226e4ed6c09695f9
export GTEST_PATH=/home/jonas/gtest_stuff
export SEMATICSOLVER=/home/jonas/code/yoda/solver_release

# Some aliases
alias ll="ls -Glah"
alias gitk="gitk --all"
alias where="type -a"
alias rm="rm -i"
alias eg="env | grep"
alias hg="history | grep"
alias vimr="vim -R"

highlight () {
  grep --color=always -iE "$1|$" | less -r
}

